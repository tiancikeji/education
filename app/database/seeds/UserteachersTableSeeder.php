<?php

class UserteachersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('userteachers')->truncate();

		$userteachers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('userteachers')->insert($userteachers);
	}

}
