<?php

class Plan_tasksTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('plan_tasks')->truncate();

		$plan_tasks = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('plan_tasks')->insert($plan_tasks);
	}

}
